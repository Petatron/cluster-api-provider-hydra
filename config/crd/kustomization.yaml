# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
# The contract-version label is mandatory for Cluster API infrastructure providers.
# Cluster API reads it to work out which of this CRD's served versions implements
# the v1beta2 contract, so it can resolve object references. It is distinct from the
# cluster.x-k8s.io/provider ownership label applied in config/default, and one does
# not substitute for the other. Must stay in step with metadata.yaml.
labels:
- includeSelectors: false
  pairs:
    cluster.x-k8s.io/v1beta2: v1alpha1

resources:
- bases/infrastructure.cluster.x-k8s.io_hydramachines.yaml
- bases/infrastructure.cluster.x-k8s.io_hydramachinetemplates.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [WEBHOOK] To enable webhook, uncomment the following section
# the following config is for teaching kustomize how to do kustomization for CRDs.
#configurations:
#- kustomizeconfig.yaml
