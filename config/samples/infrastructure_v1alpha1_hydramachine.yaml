apiVersion: infrastructure.cluster.x-k8s.io/v1alpha1
kind: HydraMachine
metadata:
  labels:
    app.kubernetes.io/name: cluster-api-provider-hydra
    app.kubernetes.io/managed-by: kustomize
  name: hydramachine-sample
# spec is required by the CRD schema, so it must be an explicit empty mapping
# rather than a comment. PET-7 replaces this with real fields.
spec: {}
